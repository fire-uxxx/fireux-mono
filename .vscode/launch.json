{
  // Run & Debug targets backed by the tasks
  "version": "0.2.0",
  "configurations": [
    {
      "name": "FireUX dev (3000)",
      "type": "node-terminal",
      "request": "launch",
      "command": "pnpm dev:fireux",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "MiseBox dev (3001)",
      "type": "node-terminal",
      "request": "launch",
      "command": "pnpm dev:misebox",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "CleanBox dev (3002)",
      "type": "node-terminal",
      "request": "launch",
      "command": "pnpm dev:cleanbox",
      "cwd": "${workspaceFolder}"
    }
  ],
  "compounds": [
    {
      "name": "All dev servers (3000+3001+3002)",
      "configurations": [
        "FireUX dev (3000)",
        "MiseBox dev (3001)",
        "CleanBox dev (3002)"
      ]
    }
  ]
}
