##############################
# Minimal Shared Env (Single Source)
# Use SAME values across apps for zero config deltas.
# Only change APP_ID if you truly need per-app identity.
##############################

# Firebase (all required)
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_PROJECT_ID=
FIREBASE_STORAGE_BUCKET=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_APP_ID=

# App identity (keep unified unless differentiation required)
APP_ID=fireux-app
APP_NAME=FireUX
APP_SHORT_NAME=FireUX
APP_PRIMARY_COLOR=#0EA5E9

# (Optional service account path if using Admin features locally)
# GOOGLE_APPLICATION_CREDENTIALS=./packages/tenants/<tenant>/config/service-account.json

# Anything else removed intentionally for now to enforce zero-delta setup.

