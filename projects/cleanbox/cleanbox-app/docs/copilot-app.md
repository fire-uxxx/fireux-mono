# Cleanbox App — Copilot Notes

 Nuxt app using FireUX modules. Config via `createFireuxConfig` in `nuxt.config.ts`.
 Local env: add `.env` with Firebase client keys.
 Run: `pnpm dev:cleanbox` from repo root.
