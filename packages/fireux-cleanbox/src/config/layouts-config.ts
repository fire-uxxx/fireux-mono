import { addLayout } from '@nuxt/kit'
import type { Resolver } from '@nuxt/kit'

export function configureLayouts(resolver: Resolver) {
  // Add custom cleanbox layouts here when needed
  console.log('🧽 CleanBox layouts configured')
}
