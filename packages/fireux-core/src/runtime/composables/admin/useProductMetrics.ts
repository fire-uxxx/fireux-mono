import { computed } from 'vue'
import { useProducts } from '../firestore/objects/Product/useProducts'
import type { FirebaseProduct } from '../../models/product.model'

export async function useProductMetrics() {
  return {}
}
