/**
 * Firebase Types
 *
 * Reusable type definitions for Firebase integrations
 */

/**
 * Firebase Timestamp type for compatibility
 */
export interface FirebaseTimestamp {
  seconds: number
  nanoseconds: number
}
