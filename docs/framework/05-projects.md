# Projects

Projects compose packages and theme/runtime config
Each project keeps README + copilot-instructions file
Use dev scripts to run apps in parallel

Environments:
CleanBox, MiseBox, FireUX, plus brand variants (Niederhorn, Richies)